// Définition d'une variable quantité
var quantite = 3;

var monTest = (quantite > 5);
console.log(monTest);

// Mise en place de la condition
if (monTest) {
	console.log("La livraison sera gratuite");
} else {
	console.log("La livraison coûtera 10$");
}
